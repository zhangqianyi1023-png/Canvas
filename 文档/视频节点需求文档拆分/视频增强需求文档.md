# 视频增强需求文档

## 1. 需求背景

用户在画布中生成或上传视频后，经常需要进一步提升视频质量，例如提高清晰度、调整帧率、生成慢动作版本等。

当前原型已经验证了「从视频节点打开视频增强面板，选择参数后生成下游视频节点」的交互方式。正式版本需要将该能力接入真实视频增强 API，实现真实的视频处理结果。

本需求文档面向正式版本功能开发，不要求基于当前原型代码继续开发。

## 2. 功能目标

为视频节点增加「视频增强」能力。用户可以基于已有视频选择增强参数，提交后由后端调用视频增强 API 处理视频，最终在画布中生成一个新的增强后视频节点。

核心目标：

- 不覆盖原视频。
- 增强结果作为新节点生成。
- 新节点与原视频节点自动连接。
- 用户可以清楚看到处理中、成功、失败状态。
- 第一版只支持 API 当前已有能力，不额外扩展。

## 3. 第一版能力范围

第一版视频增强只支持 API 当前提供的三个参数。

### 3.1 视频高清分辨率

可选项：

- `1080P`
- `2K`
- `4K`

说明：用于控制增强后视频的目标清晰度。

### 3.2 视频帧数

可选项：

- `自适应`，保持原帧数
- `30fps`
- `60fps`
- `90fps`

说明：用于控制增强后视频的目标帧率。帧率越高，视频运动越顺滑，但处理成本和耗时也更高。

### 3.3 视频放慢倍率

可选项：

- `自适应`，保持原速
- `2x`

说明：用于生成慢动作效果。若选择 `2x`，视频播放速度变慢，时长会相应增加，具体以 API 输出为准。

## 4. 暂不支持范围

第一版不做以下能力：

- 人脸专项修复
- 去噪专项开关
- 锐化专项开关
- 自动调色
- HDR 增强
- 去水印
- 去字幕
- 主体替换
- 背景替换
- 局部区域增强
- 手动画笔选择增强区域
- 多段视频合并增强
- 本地模型推理

以上能力可以作为后续版本扩展，不纳入第一版验收范围。

## 5. 入口位置

视频增强入口位于视频节点工具栏中。

当节点满足以下条件时显示入口：

- 节点是视频结果节点，或上传视频节点。
- 节点内存在可访问的视频地址。
- 视频当前不是生成中、上传中、损坏状态。

入口名称：`视频增强`

点击后打开视频增强参数面板。

## 6. 用户流程

1. 用户在画布中选中一个视频节点。
2. 用户点击节点工具栏中的「视频增强」。
3. 系统打开视频增强面板。
4. 用户选择视频高清分辨率、视频帧数、视频放慢倍率。
5. 系统展示预计消耗积分。
6. 用户点击生成按钮。
7. 前端提交视频增强任务。
8. 后端调用视频增强 API。
9. 前端展示任务状态。
10. 任务成功后，画布中生成一个新的视频节点。
11. 新节点自动连接到原视频节点。
12. 用户可以播放、下载、继续编辑增强后视频。

## 7. 画布节点规则

视频增强结果必须生成新节点，不覆盖原节点。

原因：

- 用户可以保留原始视频。
- 用户可以对比增强前后效果。
- 增强失败不会破坏原视频。
- 符合画布「输入节点 -> 处理节点/结果节点」的逻辑。

生成结果节点规则：

- 节点类型：视频结果节点。
- 默认标题：`视频增强`。
- 节点内容：增强后视频。
- 节点位置：原视频节点右侧或下游位置。
- 连线关系：原视频节点 -> 视频增强结果节点。
- 节点状态：成功后为普通视频结果状态。

处理中节点建议采用以下方式：

- 提交后立即创建一个下游「处理中」视频节点。
- 任务成功后，将该节点更新为真实视频结果。
- 任务失败后，该节点显示失败状态，并提供重试入口。

该方案更符合画布产品逻辑，用户可以明确看到任务流向。

## 8. 视频增强面板

面板标题：`视频增强`

参数区：

- 视频高清分辨率：`1080P`、`2K`、`4K`
- 视频帧数：`自适应`、`30fps`、`60fps`、`90fps`
- 视频放慢倍率：`自适应`、`2x`

底部操作区：

- 预计积分
- 生成按钮
- 关闭按钮

状态提示：

- 提交中
- 排队中
- 处理中
- 增强完成
- 增强失败

## 9. 任务状态

视频增强任务需要支持以下状态：

| 状态 | 含义 |
| --- | --- |
| `idle` | 未开始 |
| `submitting` | 前端正在提交任务 |
| `queued` | 任务已创建，等待 API 处理 |
| `processing` | API 正在处理视频 |
| `succeeded` | 任务成功，已获得增强后视频 |
| `failed` | 任务失败 |
| `canceled` | 用户取消任务，或系统取消任务 |

前端展示建议：

- `submitting`：生成按钮 loading。
- `queued`：显示「排队中」。
- `processing`：显示「处理中」，如果 API 有进度则显示进度。
- `succeeded`：显示增强后视频。
- `failed`：显示失败原因和重试按钮。
- `canceled`：显示「已取消」。

## 10. 接口需求

正式版本建议由后端统一封装第三方视频增强 API，前端不要直接调用第三方 API。

原因：

- API Key 不能暴露在前端。
- 后端需要统一处理积分、任务、日志、失败重试。
- 后端需要保存结果视频。
- 后端需要屏蔽不同供应商 API 差异。

### 10.1 创建视频增强任务

```http
POST /api/video-enhancement/tasks
```

请求参数：

```json
{
  "projectId": "project_xxx",
  "sourceNodeId": "node_xxx",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "resolution": "1080P",
  "fps": "60fps",
  "slow": "自适应",
  "credits": 26
}
```

字段说明：

- `projectId`：当前项目 ID。
- `sourceNodeId`：原视频节点 ID。
- `sourceVideoUrl`：原视频地址。
- `resolution`：目标分辨率。
- `fps`：目标帧率。
- `slow`：放慢倍率。
- `credits`：前端展示的预计积分，最终以后端计算为准。

返回：

```json
{
  "taskId": "task_xxx",
  "status": "queued",
  "resultNodeId": "node_result_xxx"
}
```

### 10.2 查询任务状态

```http
GET /api/video-enhancement/tasks/:taskId
```

返回排队中：

```json
{
  "taskId": "task_xxx",
  "status": "queued",
  "progress": 0
}
```

返回处理中：

```json
{
  "taskId": "task_xxx",
  "status": "processing",
  "progress": 45
}
```

返回成功：

```json
{
  "taskId": "task_xxx",
  "status": "succeeded",
  "videoUrl": "https://example.com/enhanced.mp4",
  "duration": 8.2,
  "width": 1920,
  "height": 1080,
  "fps": 60,
  "fileSize": 12345678
}
```

返回失败：

```json
{
  "taskId": "task_xxx",
  "status": "failed",
  "errorCode": "VIDEO_ENHANCEMENT_FAILED",
  "errorMessage": "视频增强失败，请稍后重试"
}
```

### 10.3 取消任务

```http
POST /api/video-enhancement/tasks/:taskId/cancel
```

返回：

```json
{
  "taskId": "task_xxx",
  "status": "canceled"
}
```

## 11. 数据结构建议

原视频节点不需要被覆盖。

增强结果节点建议保存：

```json
{
  "label": "视频增强",
  "resultType": "generateVideo",
  "videoUrl": "https://example.com/enhanced.mp4",
  "videoSource": "video-enhancement",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "sourceNodeId": "node_xxx",
  "operation": "video-enhancement",
  "videoEnhancement": {
    "taskId": "task_xxx",
    "status": "succeeded",
    "resolution": "1080P",
    "fps": "60fps",
    "slow": "自适应",
    "credits": 26,
    "duration": 8.2,
    "width": 1920,
    "height": 1080,
    "createdAt": 1234567890
  }
}
```

处理中节点建议保存：

```json
{
  "label": "视频增强",
  "resultType": "generateVideo",
  "videoUrl": "",
  "videoSource": "video-enhancement",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "operation": "video-enhancement",
  "generationTask": {
    "taskId": "task_xxx",
    "status": "processing",
    "progress": 45
  },
  "videoEnhancement": {
    "resolution": "1080P",
    "fps": "60fps",
    "slow": "自适应",
    "credits": 26
  }
}
```

## 12. 积分规则

第一版可以沿用原型中的积分估算逻辑，但正式版本以后端计算为准。

建议规则：

- 前端展示预计积分。
- 后端提交任务前校验用户积分。
- 任务创建成功后冻结积分。
- 任务成功后正式扣除积分。
- 任务失败或取消后释放冻结积分。
- 如果第三方 API 已经开始处理且无法取消，是否退积分由业务规则决定，需要产品确认。

前端不能只相信本地积分计算，必须以后端返回为准。

## 13. 异常处理

| 场景 | 提示文案 |
| --- | --- |
| 原视频不存在 | 当前视频不可用，无法增强 |
| 视频地址无法访问 | 无法读取原视频，请重新上传或重新生成视频 |
| 文件过大 | 视频文件过大，暂不支持增强 |
| 视频过长 | 视频时长超过当前增强上限 |
| API 提交失败 | 视频增强任务提交失败，请稍后重试 |
| API 处理失败 | 视频增强失败，请调整参数后重试 |
| 积分不足 | 积分不足，无法提交视频增强 |
| 任务超时 | 视频增强处理超时，请稍后查看任务结果或重新提交 |

## 14. 限制规则

第一版建议增加明确限制，避免成本和失败率不可控。

建议限制：

- 支持格式：`MP4`、`MOV`、`WebM`
- 最大时长：建议先限制 30 秒或 60 秒
- 最大文件大小：根据 API 限制确定
- 最大分辨率输入：根据 API 限制确定
- 不支持损坏视频
- 不支持无视频轨文件
- 慢动作和高帧率同时开启时，成本需要更高

如果 API 有自己的限制，以 API 限制为准。

## 15. 任务完成后的行为

任务成功后：

- 更新处理中节点为成功节点，或创建成功结果节点。
- 填入增强后视频地址。
- 显示视频封面或可播放预览。
- 保留增强参数。
- 保留与原节点的连线。
- 支持下载。
- 支持添加到素材库。
- 支持继续接入视频编辑器。
- 支持作为下游视频生成/处理的输入。

## 16. 任务失败后的行为

任务失败后：

- 不影响原视频节点。
- 失败节点保留在画布上，显示失败状态。
- 显示失败原因。
- 提供重试入口。
- 用户可以关闭或删除失败节点。
- 失败任务不应显示为成功视频。

## 17. 埋点建议

建议记录以下事件：

- 打开视频增强面板
- 修改分辨率
- 修改帧率
- 修改放慢倍率
- 点击生成
- 任务提交成功
- 任务提交失败
- 任务处理成功
- 任务处理失败
- 点击重试
- 下载增强视频
- 将增强视频加入视频编辑器

这些数据能帮助判断用户最常选的参数和 API 成本是否合理。

## 18. 验收标准

第一版验收需要满足：

1. 视频节点可以打开视频增强面板。
2. 面板中包含分辨率、帧数、放慢倍率三个参数。
3. 用户点击生成后，会真实创建视频增强任务。
4. 后端会调用真实视频增强 API。
5. 前端能展示任务状态。
6. 任务成功后，画布中出现增强后视频节点。
7. 新节点播放的是增强后视频，不是原视频。
8. 原视频节点不会被覆盖。
9. 原视频节点和增强结果节点自动连接。
10. 失败时展示明确错误信息。
11. 积分不足时不能提交任务。
12. 任务结果刷新页面后仍能保留。
13. 增强后视频可以继续下载、编辑、进入视频编辑器。
14. 参数记录可在节点数据中追溯。

## 19. 第一版推荐实现方案

建议第一版采用：

- 保留当前原型验证过的交互。
- 点击生成后立即创建一个下游「处理中」视频节点。
- 后端创建任务并调用视频增强 API。
- 前端轮询任务状态。
- 成功后把处理中节点更新为真实视频结果。
- 失败后把节点更新为失败状态，可重试。

这条方案最符合画布产品逻辑，也最容易让用户理解「原视频经过增强生成了一个新视频」。

## 20. 一句话总结

视频增强正式版要把当前原型从「选择参数后复制原视频生成假节点」，升级为「选择参数后调用真实视频增强 API，生成一个新的增强后视频节点」。第一版只实现 API 已支持的分辨率、帧数、放慢倍率三项能力，不覆盖原视频，结果以新节点形式出现在画布中。
