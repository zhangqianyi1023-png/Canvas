# 视频主体替换需求文档

## 1. 需求背景

用户在画布中生成或上传视频后，可能希望对视频中的某个主体进行替换。例如：

- 将视频中的某个商品替换成另一个商品。
- 将人物手中的物品替换成指定物品。
- 将画面中的某个角色、道具、动物、包装、装饰物替换成用户上传或画布中已有的图片素材。

本功能需要通过真实视频编辑 API 或视频分割/跟踪模型，实现可用的主体识别、动态遮罩和主体替换。

## 2. 功能目标

为视频节点增加「主体替换」能力。用户可以在视频中框选一个要替换的主体，系统识别并跟踪该主体，用户选择替换图片后，系统调用视频编辑能力生成替换后的视频，并在画布中生成新的结果节点。

核心目标：

- 不覆盖原视频。
- 用户可以明确选择被替换主体。
- 用户可以选择替换图片。
- 系统需要尽量保持原视频的动作、镜头、背景和时长。
- 替换结果作为新的视频节点生成。
- 新节点与原视频节点自动连接。
- 用户可以看到识别、处理中、成功、失败状态。

## 3. 第一版能力范围

第一版只做「单主体替换」。

支持：

- 从视频某一帧中框选主体。
- 识别用户框选的主体。
- 展示被识别主体的预览。
- 用户选择替换图片。
- 调用后端/API 生成主体替换后的视频。
- 结果以新视频节点形式出现在画布中。

第一版暂不支持：

- 一次替换多个主体。
- 多个时间段替换不同主体。
- 用户逐帧修正遮罩。
- 多主体自动列表识别。
- 高级遮罩编辑。
- 指定复杂动作迁移。
- 替换后再自动生成音频或字幕。
- 本地模型推理。

## 4. 核心交互流程

1. 用户选中一个视频节点。
2. 用户点击节点工具栏中的「主体替换」。
3. 系统打开主体替换面板。
4. 用户点击「选择主体」。
5. 视频进入主体选择模式。
6. 用户在视频画面中框选一个主体。
7. 系统进入「识别中」状态。
8. 系统识别框选区域内的主体，并生成主体预览。
9. 用户确认被替换主体。
10. 用户选择替换图片。
11. 用户点击生成。
12. 系统提交主体替换任务。
13. 画布中出现一个下游「处理中」视频节点。
14. 任务成功后，下游节点更新为替换后视频。
15. 任务失败后，下游节点显示失败原因，并提供重试入口。

## 5. 画布节点规则

主体替换结果必须生成新节点，不覆盖原视频节点。

原因：

- 用户可以保留原始视频。
- 用户可以对比替换前后效果。
- 失败不会破坏原视频。
- 符合画布「上游素材 -> 处理结果」的逻辑。

生成结果节点规则：

- 节点类型：视频结果节点。
- 默认标题：`主体替换`。
- 节点内容：替换后视频。
- 节点位置：原视频节点右侧或下游位置。
- 连线关系：原视频节点 -> 主体替换结果节点。
- 节点状态：处理中、成功、失败。

建议提交任务后立即创建一个下游「处理中」节点。任务完成后更新该节点，而不是等成功后才创建节点。

## 6. 主体选择与识别

### 7.1 用户框选

用户在视频画面上拖拽生成一个框选区域。框选区域用于告诉系统：用户大概想替换哪个主体。

框选信息需要保存为相对坐标，避免受播放器尺寸影响。

示例：

```json
{
  "x": 32.5,
  "y": 18.2,
  "width": 21.4,
  "height": 36.8,
  "unit": "percent",
  "frameTime": 1.24
}
```

字段说明：

- `x`：框选区域左上角横坐标，占视频宽度百分比。
- `y`：框选区域左上角纵坐标，占视频高度百分比。
- `width`：框选区域宽度，占视频宽度百分比。
- `height`：框选区域高度，占视频高度百分比。
- `frameTime`：用户框选时的视频时间点。

### 7.2 主体识别

框选不是最终遮罩。框选只是提示系统「主体大概在这里」。

正式版本需要调用 API 或模型，根据框选区域识别主体轮廓，得到更精确的主体 mask。

识别成功后，前端需要展示：

- 被识别主体预览。
- 识别状态。
- 重新选择主体入口。

### 7.3 动态遮罩预览

如果 API 支持主体跟踪和动态遮罩，用户播放视频时，可以看到遮罩跟随主体运动。

这里的技术本质不是一个静态遮罩在移动，而是系统对视频每一帧或关键帧生成对应的主体 mask，并在播放时按时间同步显示。

第一版是否必须支持动态遮罩预览，需要根据 API 能力决定。

建议：

- 如果 API 支持动态 mask 预览，则展示。
- 如果 API 不支持动态 mask 预览，第一版至少展示框选帧的主体预览，并在生成前提示「将根据框选主体进行全视频替换」。

## 7. 替换图片来源

用户可以选择替换图片。

图片来源建议支持：

- 本地上传。
- 画布中已有图片节点。
- 图片结果节点。
- 素材库图片。

第一版至少支持：

- 本地上传。
- 画布中已有图片。

替换图片需要保存：

```json
{
  "targetImageUrl": "https://example.com/product.png",
  "targetImageSource": "upload",
  "targetImageNodeId": "node_xxx"
}
```

## 8. 技术实现方案分析

主体替换的真实实现有多种路线。下面几种方案都需要写入正式技术评估。

### 9.1 方案 A：视频编辑 API 一体化实现

这是最理想的方案。

如果第三方 API 支持以下输入：

- 原视频
- 用户框选区域
- 框选时间点
- 替换图片
- 替换提示词

并且 API 内部可以完成：

- 主体识别
- 主体跟踪
- 动态遮罩生成
- 视频局部重绘
- 视频合成输出

那么正式版本只需要由后端封装该 API。

优点：

- 开发成本最低。
- 前后端逻辑最清晰。
- 不需要自己维护视频分割和跟踪模型。
- 失败、耗时、效果主要由供应商负责。

缺点：

- 强依赖供应商能力。
- 可控性取决于 API 参数。
- 如果 API 对主体识别不稳定，产品侧难以修正。
- 成本和处理时长可能较高。

适用条件：

- API 明确支持视频主体替换。
- API 支持输入框选区域或 mask。
- API 支持替换图片作为参考。
- API 输出视频结果。

### 9.2 方案 B：后端先做视频主体分割/跟踪，再调用视频编辑 API

如果视频编辑 API 不支持「用户框选后自动跟踪主体」，但支持「输入视频 + mask + 替换图片」进行局部编辑，则需要后端自己生成动态 mask。

流程：

1. 用户框选主体。
2. 后端调用视频分割/跟踪模型。
3. 模型生成全视频动态 mask。
4. 后端将原视频、动态 mask、替换图片提交给视频编辑 API。
5. 视频编辑 API 输出替换后视频。

优点：

- 主体选择更可控。
- 可单独优化主体识别和遮罩质量。
- 后续可以支持遮罩预览和手动修正。

缺点：

- 技术复杂度更高。
- 需要额外模型服务。
- 处理时间更长。
- 需要存储 mask 数据或 mask 视频。
- 工程成本明显高于方案 A。

适用条件：

- 替换 API 支持 mask 输入。
- 团队有能力部署或接入视频分割/跟踪服务。
- 产品对主体选择准确性要求较高。

### 9.3 方案 C：只用生成式视频编辑模型，根据提示词重绘

如果 API 只支持「输入视频 + 提示词 + 参考图片」，不支持精确 mask 或主体跟踪，那么可以尝试让模型自行理解并替换主体。

流程：

1. 用户框选主体。
2. 系统将框选信息、主体描述、替换图片传给视频生成模型。
3. 模型生成替换后视频。

优点：

- 接入简单。
- 不需要单独 mask 服务。
- 如果模型能力足够强，能快速出结果。

缺点：

- 可控性最差。
- 容易替错主体。
- 背景、镜头、动作可能漂移。
- 替换主体可能不像用户给的图片。
- 很难保证商业场景稳定可用。

适用条件：

- 第一版只是轻量试验。
- 对结果准确性要求不高。
- API 暂不支持 mask，但支持视频编辑类生成。

不建议作为正式商业功能的唯一方案，除非 API 实测效果稳定。

## 9. Seedance 2.0 能否支持

不能默认认为 Seedance 2.0 天然支持完整主体替换。

需要以 API 文档和实测为准。

需要确认 API 是否支持以下能力：

- 输入原视频。
- 输入用户框选区域。
- 输入替换图片。
- 输入 mask 图片或 mask 视频。
- 根据框选主体生成动态遮罩。
- 对视频局部区域进行替换。
- 保持原视频动作、镜头、背景、时长。
- 输出替换后视频。

如果 API 只支持文生视频、图生视频、首尾帧视频、参考图生成视频，则不能认为它已经支持精确主体替换。

如果 API 文档中出现类似能力，则可以进一步评估：

- video editing
- video inpainting
- object replacement
- subject replacement
- mask-guided editing
- bounding box input
- reference image replacement
- video object segmentation

正式需求中建议使用中立表述：

> 主体替换依赖视频编辑 API 或视频分割/跟踪模型。若目标 API 已支持基于框选区域的主体识别、动态遮罩和视频局部替换，则优先采用 API 一体化方案；若 API 不支持动态主体跟踪，则需由后端引入独立的视频分割/跟踪服务生成 mask 后再调用视频编辑能力。

## 10. 动态遮罩能力说明

动态遮罩是该功能的关键体验之一。

用户看到的效果是：

- 用户框选视频中的一个主体。
- 系统识别主体。
- 播放视频时，遮罩跟随主体运动。

真实技术上通常是：

- 用户框选某一帧。
- 模型在该帧分割主体。
- 模型沿时间轴追踪该主体。
- 每一帧或关键帧生成一个 mask。
- 前端播放视频时按时间叠加 mask。

动态遮罩可能有两种数据形式：

### 11.1 mask 视频

后端返回一个与原视频同尺寸、同时长的黑白 mask 视频。

优点：

- 前端播放同步简单。
- 适合预览遮罩。

缺点：

- 文件体积较大。
- 需要额外存储。

### 11.2 mask 关键帧数据

后端返回一组时间点和对应 mask 数据。

优点：

- 数据量可能更小。
- 后续可支持编辑。

缺点：

- 前端实现更复杂。
- 播放时需要插值或同步渲染。

第一版建议优先让 API 或后端返回 mask 视频预览。如果 API 不支持，则可以不做动态遮罩预览，只展示主体识别结果。

## 11. 推荐第一版方案

第一版推荐优先采用方案 A。

即：

- 后端封装第三方视频主体替换 API。
- 前端传原视频、框选区域、框选时间点、替换图片。
- API 负责主体识别、动态遮罩和视频替换。
- 成功后返回替换后视频。

如果 API 不支持完整能力，则降级为方案 B。

即：

- 后端先通过视频分割/跟踪服务生成动态 mask。
- 再将原视频、mask、替换图片交给视频编辑 API。

不建议第一版直接采用方案 C 作为正式能力，除非实测证明效果稳定。

## 12. 接口需求

正式版本建议由后端封装第三方能力，前端不直接调用模型供应商 API。

### 13.1 创建主体识别任务

如果识别和生成拆成两步，建议先创建主体识别任务。

```http
POST /api/video-subject-replacement/recognize
```

请求参数：

```json
{
  "projectId": "project_xxx",
  "sourceNodeId": "node_xxx",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "frameTime": 1.24,
  "selectionRect": {
    "x": 32.5,
    "y": 18.2,
    "width": 21.4,
    "height": 36.8,
    "unit": "percent"
  }
}
```

返回：

```json
{
  "recognitionId": "recognition_xxx",
  "status": "processing"
}
```

查询识别结果：

```http
GET /api/video-subject-replacement/recognize/:recognitionId
```

成功返回：

```json
{
  "recognitionId": "recognition_xxx",
  "status": "succeeded",
  "subjectPreviewUrl": "https://example.com/subject-preview.png",
  "maskPreviewUrl": "https://example.com/mask-preview.png",
  "maskVideoUrl": "https://example.com/mask-preview.mp4",
  "subjectLabel": "已识别主体"
}
```

### 13.2 创建主体替换任务

```http
POST /api/video-subject-replacement/tasks
```

请求参数：

```json
{
  "projectId": "project_xxx",
  "sourceNodeId": "node_xxx",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "recognitionId": "recognition_xxx",
  "selectionRect": {
    "x": 32.5,
    "y": 18.2,
    "width": 21.4,
    "height": 36.8,
    "unit": "percent"
  },
  "frameTime": 1.24,
  "targetImageUrl": "https://example.com/replacement.png",
  "prompt": "将框选主体替换为参考图片中的商品，保持原视频背景、镜头运动和光照一致",
  "credits": 40
}
```

返回：

```json
{
  "taskId": "task_xxx",
  "status": "queued",
  "resultNodeId": "node_result_xxx"
}
```

### 13.3 查询主体替换任务

```http
GET /api/video-subject-replacement/tasks/:taskId
```

返回处理中：

```json
{
  "taskId": "task_xxx",
  "status": "processing",
  "progress": 42
}
```

返回成功：

```json
{
  "taskId": "task_xxx",
  "status": "succeeded",
  "videoUrl": "https://example.com/replaced-video.mp4",
  "duration": 6.2,
  "width": 1280,
  "height": 720,
  "fps": 30
}
```

返回失败：

```json
{
  "taskId": "task_xxx",
  "status": "failed",
  "errorCode": "SUBJECT_REPLACEMENT_FAILED",
  "errorMessage": "主体替换失败，请重新选择主体或更换替换图片"
}
```

## 13. 数据结构建议

处理中节点建议保存：

```json
{
  "label": "主体替换",
  "resultType": "generateVideo",
  "videoUrl": "",
  "videoSource": "video-subject-replacement",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "sourceNodeId": "node_xxx",
  "operation": "video-subject-replacement",
  "generationTask": {
    "taskId": "task_xxx",
    "status": "processing",
    "progress": 42
  },
  "videoSubjectReplacement": {
    "recognitionId": "recognition_xxx",
    "selectionRect": {
      "x": 32.5,
      "y": 18.2,
      "width": 21.4,
      "height": 36.8,
      "unit": "percent"
    },
    "frameTime": 1.24,
    "subjectPreviewUrl": "https://example.com/subject-preview.png",
    "maskPreviewUrl": "https://example.com/mask-preview.png",
    "maskVideoUrl": "https://example.com/mask-preview.mp4",
    "targetImageUrl": "https://example.com/replacement.png",
    "credits": 40
  }
}
```

成功节点建议保存：

```json
{
  "label": "主体替换",
  "resultType": "generateVideo",
  "videoUrl": "https://example.com/replaced-video.mp4",
  "videoSource": "video-subject-replacement",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "sourceNodeId": "node_xxx",
  "operation": "video-subject-replacement",
  "videoSubjectReplacement": {
    "taskId": "task_xxx",
    "recognitionId": "recognition_xxx",
    "status": "succeeded",
    "selectionRect": {
      "x": 32.5,
      "y": 18.2,
      "width": 21.4,
      "height": 36.8,
      "unit": "percent"
    },
    "frameTime": 1.24,
    "targetImageUrl": "https://example.com/replacement.png",
    "credits": 40,
    "createdAt": 1234567890
  }
}
```

## 14. 任务状态

主体替换任务需要支持以下状态：

| 状态 | 含义 |
| --- | --- |
| `idle` | 未开始 |
| `recognizing` | 正在识别主体 |
| `recognized` | 主体识别完成 |
| `submitting` | 正在提交生成任务 |
| `queued` | 任务排队中 |
| `processing` | 视频替换处理中 |
| `succeeded` | 任务成功 |
| `failed` | 任务失败 |
| `canceled` | 任务取消 |

## 15. 异常处理

| 场景 | 提示文案 |
| --- | --- |
| 原视频不可用 | 当前视频不可用，无法进行主体替换 |
| 未选择主体 | 请先在视频中选择要替换的主体 |
| 主体识别失败 | 主体识别失败，请重新框选更清晰的区域 |
| 未选择替换图片 | 请先选择替换图片 |
| 替换图片不可用 | 替换图片无法读取，请重新上传或选择其他图片 |
| 视频过长 | 视频时长超过当前主体替换上限 |
| API 提交失败 | 主体替换任务提交失败，请稍后重试 |
| API 处理失败 | 主体替换失败，请重新选择主体或更换替换图片 |
| 积分不足 | 积分不足，无法提交主体替换 |

## 16. 限制规则

第一版建议增加明确限制：

- 只支持单主体替换。
- 支持视频格式：`MP4`、`MOV`、`WebM`。
- 支持替换图片格式：`JPEG`、`PNG`、`WebP`。
- 最大视频时长建议先限制为 10 秒、30 秒或按 API 限制确定。
- 最大视频文件大小按 API 限制确定。
- 不支持严重遮挡、快速出画、主体频繁变形的视频。
- 不支持主体与背景边界极不清晰的视频。
- 不支持同时替换多个主体。

## 17. 积分规则

建议规则：

- 主体识别阶段可不扣积分，或按极低成本计费。
- 正式提交主体替换任务时展示预计积分。
- 任务创建成功后冻结积分。
- 任务成功后正式扣除积分。
- 任务失败或取消后释放冻结积分。
- 如果第三方 API 已经开始处理且无法取消，是否退积分由业务规则决定。

最终积分以后端计算为准，前端只负责展示预计值。

## 18. 验收标准

第一版验收需要满足：

1. 视频节点可以打开主体替换面板。
2. 用户可以在视频中框选要替换的主体。
3. 系统可以展示被识别主体的预览。
4. 用户可以选择替换图片。
5. 点击生成后，会真实创建主体替换任务。
6. 后端会调用真实视频主体替换 API 或相关模型服务。
7. 任务处理中，画布中有明确的处理中状态。
8. 任务成功后，画布中出现替换后视频节点。
9. 新节点播放的是替换后视频，不是原视频。
10. 原视频节点不会被覆盖。
11. 原视频节点和主体替换结果节点自动连接。
12. 任务失败时展示明确失败原因。
13. 用户可以重试。
14. 参数、框选区域、替换图片和任务 ID 可以在节点数据中追溯。

## 19. 待确认问题

正式开发前必须确认以下问题：

1. 目标 API 是否支持视频主体替换？
2. API 是否支持输入框选区域？
3. API 是否支持输入 mask？
4. API 是否可以返回动态 mask 预览？
5. API 是否要求先上传视频到供应商侧？
6. API 是否支持替换图片作为参考？
7. API 输出结果是否能保持原视频时长？
8. API 是否支持进度查询？
9. API 是否支持取消任务？
10. API 对视频时长、大小、格式有什么限制？
11. API 对替换图片有什么限制？
12. 如果 API 不支持主体跟踪，是否需要后端增加视频分割/跟踪服务？

## 20. 一句话总结

视频主体替换需要实现「用户框选视频主体，系统识别并跟踪主体，基于替换图片调用视频编辑 API，生成真实替换后视频节点」。技术上优先采用 API 一体化方案；如果 API 不支持动态主体识别和遮罩跟踪，则需要后端补充视频分割/跟踪能力。
