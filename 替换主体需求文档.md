# 视频替换主体需求文档

## 1. 功能目标

在视频节点上提供「替换主体」能力，让用户可以选择视频中的某个人或物体，并用另一张图片中的主体进行替换，生成一个新的替换后视频。

该功能重点解决：

1. 用户能明确指定“视频里要替换的主体”。
2. 用户能指定“替换成什么主体”。
3. 用户在生成前能清楚看到两边选择结果。
4. 视频播放时，被替换主体有绿色遮罩提示，帮助用户确认选择是否正确。

## 2. 功能入口

入口位置：视频节点悬浮工具栏。

按钮名称：替换主体。

点击后：

1. 在当前视频节点下方打开「替换主体」浮层。
2. 浮层跟随视频节点位置。
3. 浮层顶部显示标题和关闭按钮。
4. 再次点击工具栏按钮，或点击浮层关闭按钮，可以关闭浮层。
5. 关闭浮层不主动删除已选择内容，除非用户点击删除按钮清空选择。

## 3. 浮层结构

浮层标题：替换主体。

浮层主体分为左右两栏：

1. 左侧：被替换主体。
2. 右侧：替换主体。

左右两栏中间可以使用箭头，表达“从 A 替换为 B”的关系。

浮层底部右侧展示：

1. 本次生成预计消耗积分。
2. 生成按钮。

## 4. 被替换主体选择流程

用户点击左侧「选择主体」按钮后，进入视频框选状态。

框选状态要求：

1. 视频节点上出现可框选区域。
2. 用户可以拖拽框选视频中的一个区域。
3. 框选完成后，框选区域支持拖拽移动。
4. 框选区域支持拖拽边框调整大小。
5. 框选区域圆角为 14px。
6. 框选区域内的视频画面需要保持清晰。
7. 框选区域外不应显示移动鼠标样式。
8. 「确定」「取消」按钮跟随框选区域下方。

点击取消：

1. 退出框选状态。
2. 不保存本次框选。

点击确定：

1. 系统识别框选区域内的主体。
2. 左侧主体区域显示“识别中”状态。
3. 识别完成后，将主体图片回显到左侧占位区域。
4. 退出框选状态。

识别对象范围：

1. 人物。
2. 商品。
3. 车辆。
4. 动物。
5. 其他可见物体。

原型阶段可以使用框选区域截图模拟主体识别，不需要真实调用后端识别模型。

## 5. 绿色遮罩

当用户完成左侧主体选择后，视频播放区域中需要显示绿色遮罩。

绿色遮罩用途：

1. 告诉用户当前视频中被选中的主体是什么。
2. 帮助用户确认选区是否正确。
3. 在视频播放过程中保持“被替换对象”的感知。

遮罩表现建议：

1. 主体区域覆盖半透明绿色。
2. 边缘可以有绿色描边。
3. 不应遮挡视频主体到完全不可见。
4. 遮罩只表达“被替换对象”，不代表最终生成效果。

原型阶段可以用用户框选区域近似表示遮罩，不要求逐帧精准跟踪。

## 6. 替换主体选择流程

右侧「替换主体」支持两种来源：

1. 从本地上传图片。
2. 从画布中已有图片节点选择。

点击右侧选择按钮后，出现选择菜单：

1. 上传图片。
2. 从画布选择。

上传图片：

1. 用户选择本地图片。
2. 图片读取或上传后，回显到右侧占位区域。
3. 支持重新选择。

从画布选择：

1. 展示当前画布中可用图片。
2. 用户点击某张图片后，回显到右侧占位区域。
3. 如果画布中没有图片，显示空状态提示。

## 7. 删除与重选

左侧被替换主体：

1. 鼠标 hover 到主体预览图时，右上角显示删除按钮。
2. 点击删除后，清空当前选择。
3. 清空后可以重新点击「选择主体」。

右侧替换主体：

1. 鼠标 hover 到图片预览图时，右上角显示删除按钮。
2. 点击删除后，清空当前替换图片。
3. 清空后可以重新上传，或重新从画布选择。

## 8. 生成按钮状态

建议规则：

1. 如果左侧和右侧都未选择，点击生成时提示：请先选择被替换主体和替换主体。
2. 如果只选择了左侧，提示：请选择替换主体。
3. 如果只选择了右侧，提示：请选择被替换主体。
4. 两侧都选择完成后，点击生成进入生成流程。

原型阶段：

1. 不真实调用生成接口。
2. 点击生成后创建一个下游视频节点。
3. 下游视频节点与当前视频节点连线。
4. 下游节点标题或描述体现“替换主体结果”。

## 9. 积分展示

浮层右下展示本次生成预计消耗积分。

展示形式：

积分 icon + 数字 + 生成按钮。

积分规则：

1. 原型阶段可以固定展示。
2. 真实功能阶段可以根据视频时长、分辨率、模型消耗动态计算。

## 10. 异常状态

需要考虑以下异常：

1. 框选区域太小：提示用户重新框选。
2. 未识别到主体：提示“未识别到明确主体，请重新框选”。
3. 图片上传失败：提示“图片上传失败，请重试”。
4. 画布没有可选图片：显示“暂无可选画布图片”。
5. 视频未加载完成：选择主体按钮暂不可用，或提示稍后再试。

## 11. 原型阶段边界

当前阶段只做可交互原型，不做真实能力。

原型需要支持：

1. 打开和关闭浮层。
2. 视频区域框选。
3. 框选确认和取消。
4. 框选区域移动和调整大小。
5. 主体预览回显。
6. 绿色遮罩展示。
7. 上传或从画布选择替换图片。
8. 删除和重选主体。
9. 点击生成后创建下游视频节点。

原型不做：

1. 不真实调用主体识别模型。
2. 不做真实视频主体跟踪。
3. 不做真实视频替换生成。
4. 不真实扣积分。
5. 不处理复杂遮挡、多人、多物体追踪。

## 12. 真实功能后续待确认

后续做真实能力前，需要继续确认：

1. 是否支持一次替换多个主体。
2. 绿色遮罩是只在当前帧显示，还是播放过程中持续跟踪。
3. 替换主体图片是否必须包含透明背景。
4. 替换后是否保持原主体动作、姿态、光照和透视关系。
5. 是否支持人物脸部替换，还是只支持整体主体替换。
6. 是否需要先生成短预览，再正式生成完整视频。
7. 替换失败时是否退还积分。

## 13. 当前建议

当前原型建议先保持“单主体替换”。

也就是一次只选择一个被替换主体，一个替换主体。这样交互最清楚，研发实现风险也最低，后续可以再扩展为多主体替换。
