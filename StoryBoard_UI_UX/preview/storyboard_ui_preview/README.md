# Storyboard UI Preview

无后端、无登录、无生产数据的 macOS Flutter 验收程序。

```bash
flutter pub get
flutter run -d macos
```

顶部可以切换八组 Fixture；底部 Callback Recorder 显示 UI 发出的业务意图。Preview 只验证 UI 和 Contract，不证明真实业务已接入。
