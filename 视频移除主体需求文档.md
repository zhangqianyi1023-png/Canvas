# 视频移除主体需求文档

## 1. 需求背景

用户在画布中生成或上传视频后，可能希望移除视频中的某个干扰主体。例如：

- 移除路人、杂物、道具。
- 移除画面中的多余商品或包装。
- 移除遮挡主体的物体。
- 移除不希望出现在视频中的 Logo、标识或背景元素。

本功能需要通过真实视频编辑 API 或视频分割/跟踪模型，实现可用的主体识别、动态遮罩和视频背景补全。

## 2. 功能目标

为视频节点增加「移除主体」能力。用户可以在视频中框选一个要移除的主体，系统识别并跟踪该主体，生成动态遮罩后调用视频修复能力，将主体从视频中移除，并自然补齐被遮挡的背景区域。

核心目标：

- 不覆盖原视频。
- 用户可以明确选择被移除主体。
- 系统需要尽量保持原视频的镜头、背景、时长和整体观感。
- 移除区域需要进行自然背景补全，不能只是简单涂黑、模糊或透明。
- 移除结果作为新的视频节点生成。
- 新节点与原视频节点自动连接。
- 用户可以看到识别、处理中、成功、失败状态。

## 3. 第一版能力范围

第一版只做「单主体移除」。

支持：

- 从视频某一帧中框选主体。
- 识别用户框选的主体。
- 展示被识别主体的预览。
- 调用后端/API 生成移除主体后的视频。
- 结果以新视频节点形式出现在画布中。

第一版暂不支持：

- 一次移除多个主体。
- 多个时间段移除不同主体。
- 用户逐帧修正遮罩。
- 多主体自动列表识别。
- 高级遮罩编辑。
- 指定背景补全内容。
- 手动绘制背景。
- 移除后自动调色。
- 本地模型推理。

## 4. 核心交互流程

1. 用户选中一个视频节点。
2. 用户点击节点工具栏中的「移除主体」。
3. 系统打开移除主体面板。
4. 用户点击「选择主体」。
5. 视频进入主体选择模式。
6. 用户在视频画面中框选一个主体。
7. 系统进入「识别中」状态。
8. 系统识别框选区域内的主体，并生成主体预览。
9. 用户确认被移除主体。
10. 用户点击生成。
11. 系统提交移除主体任务。
12. 画布中出现一个下游「处理中」视频节点。
13. 任务成功后，下游节点更新为移除主体后的视频。
14. 任务失败后，下游节点显示失败原因，并提供重试入口。

## 5. 画布节点规则

移除主体结果必须生成新节点，不覆盖原视频节点。

原因：

- 用户可以保留原始视频。
- 用户可以对比移除前后效果。
- 失败不会破坏原视频。
- 符合画布「上游素材 -> 处理结果」的逻辑。

生成结果节点规则：

- 节点类型：视频结果节点。
- 默认标题：`移除主体`。
- 节点内容：移除主体后的视频。
- 节点位置：原视频节点右侧或下游位置。
- 连线关系：原视频节点 -> 移除主体结果节点。
- 节点状态：处理中、成功、失败。

建议提交任务后立即创建一个下游「处理中」节点。任务完成后更新该节点，而不是等成功后才创建节点。

## 6. 主体选择与识别

### 6.1 用户框选

用户在视频画面上拖拽生成一个框选区域。框选区域用于告诉系统：用户大概想移除哪个主体。

框选信息需要保存为相对坐标，避免受播放器尺寸影响。

示例：

```json
{
  "x": 32.5,
  "y": 18.2,
  "width": 21.4,
  "height": 36.8,
  "unit": "percent",
  "frameTime": 1.24
}
```

字段说明：

- `x`：框选区域左上角横坐标，占视频宽度百分比。
- `y`：框选区域左上角纵坐标，占视频高度百分比。
- `width`：框选区域宽度，占视频宽度百分比。
- `height`：框选区域高度，占视频高度百分比。
- `frameTime`：用户框选时的视频时间点。

### 6.2 主体识别

框选不是最终遮罩。框选只是提示系统「主体大概在这里」。

正式版本需要调用 API 或模型，根据框选区域识别主体轮廓，得到更精确的主体 mask。

识别成功后，前端需要展示：

- 被识别主体预览。
- 识别状态。
- 重新选择主体入口。

### 6.3 动态遮罩

移除主体需要在整段视频中持续定位该主体，而不是只处理用户框选的那一帧。

系统需要生成动态遮罩：

- 用户在某一帧框选主体。
- 模型在该帧分割主体。
- 模型沿时间轴追踪该主体。
- 每一帧或关键帧生成对应 mask。
- 视频修复 API 基于这些 mask 做背景补全。

如果 API 支持动态 mask 预览，用户播放视频时可以看到遮罩跟随主体运动。

如果 API 不支持动态 mask 预览，第一版至少展示框选帧的主体预览，并在生成前提示「将根据框选主体进行全视频移除」。

## 7. 背景补全要求

移除主体不是简单删除画面内容。系统需要对主体遮挡区域进行背景补全。

背景补全需要尽量满足：

- 与周围背景纹理一致。
- 与原视频光照一致。
- 与原视频镜头运动一致。
- 前后帧稳定，不明显闪烁。
- 不产生大面积糊块、黑块、透明区域。
- 不明显扭曲背景中的线条、文字、人物或商品。

容易成功的场景：

- 主体较小。
- 背景简单。
- 主体运动较慢。
- 主体遮挡区域不复杂。
- 视频时长较短。

容易失败的场景：

- 主体很大。
- 主体遮挡复杂背景。
- 主体快速运动。
- 镜头快速移动。
- 背景中有文字、图案、人脸、商品包装。
- 主体频繁出画入画。
- 主体和背景边界不清晰。

正式产品需要在功能说明或失败提示中体现这些限制，避免用户以为任何主体都可以完美移除。

## 8. 技术实现方案分析

移除主体与主体替换的前半段能力高度相似，都依赖「主体识别 + 动态遮罩 + 视频局部编辑」。区别在最后一步：

- 主体替换：将遮罩区域替换成新的主体。
- 移除主体：将遮罩区域删除，并用背景自然补齐。

### 8.1 方案 A：视频编辑 API 一体化实现

这是最理想的方案。

如果第三方 API 支持以下输入：

- 原视频
- 用户框选区域
- 框选时间点
- 操作类型：移除主体

并且 API 内部可以完成：

- 主体识别
- 主体跟踪
- 动态遮罩生成
- 视频背景补全
- 视频合成输出

那么正式版本只需要由后端封装该 API。

优点：

- 开发成本最低。
- 前后端逻辑最清晰。
- 不需要自己维护视频分割、跟踪和背景补全模型。
- 失败、耗时、效果主要由供应商负责。

缺点：

- 强依赖供应商能力。
- 可控性取决于 API 参数。
- 如果 API 对主体识别或背景补全不稳定，产品侧难以修正。
- 成本和处理时长可能较高。

适用条件：

- API 明确支持视频主体移除或视频擦除。
- API 支持输入框选区域或 mask。
- API 输出移除后视频。

### 8.2 方案 B：后端先做视频主体分割/跟踪，再调用视频修复 API

如果视频修复 API 不支持「用户框选后自动跟踪主体」，但支持「输入视频 + mask」进行局部修复，则需要后端自己生成动态 mask。

流程：

1. 用户框选主体。
2. 后端调用视频分割/跟踪模型。
3. 模型生成全视频动态 mask。
4. 后端将原视频和动态 mask 提交给视频修复 API。
5. 视频修复 API 输出移除后视频。

优点：

- 主体选择更可控。
- 可单独优化主体识别和遮罩质量。
- 后续可以支持遮罩预览和手动修正。

缺点：

- 技术复杂度更高。
- 需要额外模型服务。
- 处理时间更长。
- 需要存储 mask 数据或 mask 视频。
- 工程成本明显高于方案 A。

适用条件：

- 修复 API 支持 mask 输入。
- 团队有能力部署或接入视频分割/跟踪服务。
- 产品对主体选择准确性要求较高。

### 8.3 方案 C：只用生成式视频编辑模型重绘

如果 API 只支持「输入视频 + 提示词」，不支持精确 mask 或主体跟踪，也可以尝试让模型自行理解并移除主体。

流程：

1. 用户框选主体。
2. 系统将框选信息和提示词传给视频生成模型。
3. 模型生成移除主体后的视频。

优点：

- 接入简单。
- 不需要单独 mask 服务。

缺点：

- 可控性最差。
- 容易移除错主体。
- 背景、镜头、动作可能漂移。
- 视频可能整体被重绘，而不是局部移除。
- 很难保证商业场景稳定可用。

不建议第一版直接采用方案 C 作为正式能力，除非实测证明效果稳定。

## 9. 与主体替换的关系

移除主体和主体替换可以共用以下能力：

- 视频主体框选组件。
- 主体识别逻辑。
- 动态遮罩生成。
- 视频任务创建与轮询。
- 下游结果节点创建。
- 积分计算与扣费。
- 失败重试。
- API 错误处理。

二者不同点：

| 能力 | 主体替换 | 移除主体 |
| --- | --- | --- |
| 是否需要替换图片 | 需要 | 不需要 |
| 最终处理 | 替换为新主体 | 删除主体并补背景 |
| 关键难点 | 新主体一致性 | 背景补全自然度 |
| 用户输入 | 框选主体 + 替换图片 | 框选主体 |

如果后端能力设计合理，可以抽象为统一的「视频局部编辑」任务：

```json
{
  "operation": "remove",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "selectionRect": {
    "x": 32.5,
    "y": 18.2,
    "width": 21.4,
    "height": 36.8,
    "unit": "percent"
  }
}
```

其中 `operation` 可以扩展为：

- `remove`：移除主体
- `replace`：替换主体
- `repaint`：重绘区域

## 10. Seedance 2.0 能否支持

不能默认认为 Seedance 2.0 天然支持完整主体移除。

需要以 API 文档和实测为准。

需要确认 API 是否支持以下能力：

- 输入原视频。
- 输入用户框选区域。
- 输入 mask 图片或 mask 视频。
- 根据框选主体生成动态遮罩。
- 对视频局部区域进行移除或修复。
- 保持原视频动作、镜头、背景、时长。
- 输出移除后视频。

如果 API 只支持文生视频、图生视频、首尾帧视频、参考图生成视频，则不能认为它已经支持精确主体移除。

如果 API 文档中出现类似能力，则可以进一步评估：

- video editing
- video inpainting
- object removal
- subject removal
- mask-guided editing
- bounding box input
- video object segmentation
- video completion

正式需求中建议使用中立表述：

> 移除主体依赖视频编辑 API 或视频分割/跟踪模型。若目标 API 已支持基于框选区域的主体识别、动态遮罩和视频局部修复，则优先采用 API 一体化方案；若 API 不支持动态主体跟踪，则需由后端引入独立的视频分割/跟踪服务生成 mask 后再调用视频修复能力。

## 11. 动态遮罩能力说明

动态遮罩是该功能的关键前置能力。

用户看到的效果是：

- 用户框选视频中的一个主体。
- 系统识别主体。
- 播放视频时，遮罩跟随主体运动。

真实技术上通常是：

- 用户框选某一帧。
- 模型在该帧分割主体。
- 模型沿时间轴追踪该主体。
- 每一帧或关键帧生成一个 mask。
- 视频修复模型基于 mask 对遮挡区域做背景补全。

动态遮罩可能有两种数据形式：

### 11.1 mask 视频

后端返回一个与原视频同尺寸、同时长的黑白 mask 视频。

优点：

- 前端播放同步简单。
- 适合预览遮罩。

缺点：

- 文件体积较大。
- 需要额外存储。

### 11.2 mask 关键帧数据

后端返回一组时间点和对应 mask 数据。

优点：

- 数据量可能更小。
- 后续可支持编辑。

缺点：

- 前端实现更复杂。
- 播放时需要插值或同步渲染。

第一版建议优先让 API 或后端返回 mask 视频预览。如果 API 不支持，则可以不做动态遮罩预览，只展示主体识别结果。

## 12. 推荐第一版方案

第一版推荐优先采用方案 A。

即：

- 后端封装第三方视频主体移除 API。
- 前端传原视频、框选区域、框选时间点。
- API 负责主体识别、动态遮罩和背景补全。
- 成功后返回移除主体后视频。

如果 API 不支持完整能力，则降级为方案 B。

即：

- 后端先通过视频分割/跟踪服务生成动态 mask。
- 再将原视频和 mask 交给视频修复 API。

不建议第一版直接采用方案 C 作为正式能力，除非实测证明效果稳定。

## 13. 接口需求

正式版本建议由后端封装第三方能力，前端不直接调用模型供应商 API。

### 13.1 创建主体识别任务

如果识别和生成拆成两步，建议先创建主体识别任务。

```http
POST /api/video-subject-removal/recognize
```

请求参数：

```json
{
  "projectId": "project_xxx",
  "sourceNodeId": "node_xxx",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "frameTime": 1.24,
  "selectionRect": {
    "x": 32.5,
    "y": 18.2,
    "width": 21.4,
    "height": 36.8,
    "unit": "percent"
  }
}
```

返回：

```json
{
  "recognitionId": "recognition_xxx",
  "status": "processing"
}
```

查询识别结果：

```http
GET /api/video-subject-removal/recognize/:recognitionId
```

成功返回：

```json
{
  "recognitionId": "recognition_xxx",
  "status": "succeeded",
  "subjectPreviewUrl": "https://example.com/subject-preview.png",
  "maskPreviewUrl": "https://example.com/mask-preview.png",
  "maskVideoUrl": "https://example.com/mask-preview.mp4",
  "subjectLabel": "已识别主体"
}
```

### 13.2 创建移除主体任务

```http
POST /api/video-subject-removal/tasks
```

请求参数：

```json
{
  "projectId": "project_xxx",
  "sourceNodeId": "node_xxx",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "recognitionId": "recognition_xxx",
  "selectionRect": {
    "x": 32.5,
    "y": 18.2,
    "width": 21.4,
    "height": 36.8,
    "unit": "percent"
  },
  "frameTime": 1.24,
  "prompt": "移除框选主体，并自然补全背景，保持原视频镜头运动、光照和时长一致",
  "credits": 36
}
```

返回：

```json
{
  "taskId": "task_xxx",
  "status": "queued",
  "resultNodeId": "node_result_xxx"
}
```

### 13.3 查询移除主体任务

```http
GET /api/video-subject-removal/tasks/:taskId
```

返回处理中：

```json
{
  "taskId": "task_xxx",
  "status": "processing",
  "progress": 42
}
```

返回成功：

```json
{
  "taskId": "task_xxx",
  "status": "succeeded",
  "videoUrl": "https://example.com/removed-video.mp4",
  "duration": 6.2,
  "width": 1280,
  "height": 720,
  "fps": 30
}
```

返回失败：

```json
{
  "taskId": "task_xxx",
  "status": "failed",
  "errorCode": "SUBJECT_REMOVAL_FAILED",
  "errorMessage": "主体移除失败，请重新选择主体或更换视频片段"
}
```

## 14. 数据结构建议

处理中节点建议保存：

```json
{
  "label": "移除主体",
  "resultType": "generateVideo",
  "videoUrl": "",
  "videoSource": "video-subject-removal",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "sourceNodeId": "node_xxx",
  "operation": "video-subject-removal",
  "generationTask": {
    "taskId": "task_xxx",
    "status": "processing",
    "progress": 42
  },
  "videoSubjectRemoval": {
    "recognitionId": "recognition_xxx",
    "selectionRect": {
      "x": 32.5,
      "y": 18.2,
      "width": 21.4,
      "height": 36.8,
      "unit": "percent"
    },
    "frameTime": 1.24,
    "subjectPreviewUrl": "https://example.com/subject-preview.png",
    "maskPreviewUrl": "https://example.com/mask-preview.png",
    "maskVideoUrl": "https://example.com/mask-preview.mp4",
    "credits": 36
  }
}
```

成功节点建议保存：

```json
{
  "label": "移除主体",
  "resultType": "generateVideo",
  "videoUrl": "https://example.com/removed-video.mp4",
  "videoSource": "video-subject-removal",
  "sourceVideoUrl": "https://example.com/source.mp4",
  "sourceNodeId": "node_xxx",
  "operation": "video-subject-removal",
  "videoSubjectRemoval": {
    "taskId": "task_xxx",
    "recognitionId": "recognition_xxx",
    "status": "succeeded",
    "selectionRect": {
      "x": 32.5,
      "y": 18.2,
      "width": 21.4,
      "height": 36.8,
      "unit": "percent"
    },
    "frameTime": 1.24,
    "credits": 36,
    "createdAt": 1234567890
  }
}
```

## 15. 任务状态

移除主体任务需要支持以下状态：

| 状态 | 含义 |
| --- | --- |
| `idle` | 未开始 |
| `recognizing` | 正在识别主体 |
| `recognized` | 主体识别完成 |
| `submitting` | 正在提交生成任务 |
| `queued` | 任务排队中 |
| `processing` | 视频移除处理中 |
| `succeeded` | 任务成功 |
| `failed` | 任务失败 |
| `canceled` | 任务取消 |

## 16. 异常处理

| 场景 | 提示文案 |
| --- | --- |
| 原视频不可用 | 当前视频不可用，无法进行主体移除 |
| 未选择主体 | 请先在视频中选择要移除的主体 |
| 主体识别失败 | 主体识别失败，请重新框选更清晰的区域 |
| 视频背景过于复杂 | 当前画面背景复杂，移除效果可能不稳定 |
| 视频过长 | 视频时长超过当前主体移除上限 |
| API 提交失败 | 主体移除任务提交失败，请稍后重试 |
| API 处理失败 | 主体移除失败，请重新选择主体或更换视频片段 |
| 积分不足 | 积分不足，无法提交主体移除 |

## 17. 限制规则

第一版建议增加明确限制：

- 只支持单主体移除。
- 支持视频格式：`MP4`、`MOV`、`WebM`。
- 最大视频时长建议先限制为 10 秒、30 秒或按 API 限制确定。
- 最大视频文件大小按 API 限制确定。
- 不支持严重遮挡、快速出画、主体频繁变形的视频。
- 不支持主体与背景边界极不清晰的视频。
- 不支持主体遮挡大面积复杂文字、图案或人脸的场景。
- 不支持同时移除多个主体。

## 18. 积分规则

建议规则：

- 主体识别阶段可不扣积分，或按极低成本计费。
- 正式提交移除主体任务时展示预计积分。
- 任务创建成功后冻结积分。
- 任务成功后正式扣除积分。
- 任务失败或取消后释放冻结积分。
- 如果第三方 API 已经开始处理且无法取消，是否退积分由业务规则决定。

最终积分以后端计算为准，前端只负责展示预计值。

## 19. 验收标准

第一版验收需要满足：

1. 视频节点可以打开移除主体面板。
2. 用户可以在视频中框选要移除的主体。
3. 系统可以展示被识别主体的预览。
4. 点击生成后，会真实创建移除主体任务。
5. 后端会调用真实视频主体移除 API 或相关模型服务。
6. 任务处理中，画布中有明确的处理中状态。
7. 任务成功后，画布中出现移除主体后的视频节点。
8. 新节点播放的是移除主体后的视频，不是原视频。
9. 原视频节点不会被覆盖。
10. 原视频节点和移除主体结果节点自动连接。
11. 任务失败时展示明确失败原因。
12. 用户可以重试。
13. 参数、框选区域、任务 ID 可以在节点数据中追溯。

## 20. 待确认问题

正式开发前必须确认以下问题：

1. 目标 API 是否支持视频主体移除？
2. API 是否支持输入框选区域？
3. API 是否支持输入 mask？
4. API 是否可以返回动态 mask 预览？
5. API 是否要求先上传视频到供应商侧？
6. API 输出结果是否能保持原视频时长？
7. API 是否支持进度查询？
8. API 是否支持取消任务？
9. API 对视频时长、大小、格式有什么限制？
10. 如果 API 不支持主体跟踪，是否需要后端增加视频分割/跟踪服务？
11. 如果 API 不支持背景补全，是否需要单独接视频 inpainting 服务？

## 21. 一句话总结

视频移除主体需要实现「用户框选视频主体，系统识别并跟踪主体，基于动态遮罩调用视频修复 API，生成自然补全背景后的新视频节点」。技术上优先采用 API 一体化方案；如果 API 不支持动态主体识别和遮罩跟踪，则需要后端补充视频分割/跟踪能力。
