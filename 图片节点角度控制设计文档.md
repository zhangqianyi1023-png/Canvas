# 图片节点角度控制设计文档

## 1. 功能定位

「角度控制」是图片节点工具栏中的图片编辑能力，用于基于当前图片生成同一主体的新视角图片。

用户选择一张图片后，可以通过快捷视角、3D 相机预览、水平环绕、垂直俯仰、相机距离和提示词补充来描述目标视角。点击生成后，系统不会覆盖原图片，而是在当前图片节点下游创建一个新的图片生成节点，并将原图片作为参考图进行生成。

当前产品内的命名关系如下：

- 工具栏按钮名称：角度控制
- 打开的浮层标题：多角度编辑器
- 预览控件类型：真实 3D 相机视角预览
- 生成结果：新图片节点，不覆盖原图

## 2. 入口位置

入口只出现在图片结果节点的图片工具栏中。

图片节点被选中后，图片上方出现工具栏。工具栏中的「角度控制」按钮以 icon 形式展示，hover 后显示 Tooltip「角度控制」。

适用范围：

- 支持：图片结果节点
- 不支持：视频节点、音频节点、文本节点、上传前的空图片占位、非 result 类型图片来源

如果用户正在拖拽节点，工具栏和角度控制浮层都不应该出现。拖拽结束后，根据节点最终选中状态再显示工具栏。

## 3. 打开与关闭

### 3.1 打开

用户点击图片工具栏中的「角度控制」按钮后：

1. 关闭图片节点内其他编辑模式，例如局部修改、擦除、打光、裁剪、宫格拆分等。
2. 当前图片节点进入图片编辑状态。
3. 图片工具栏隐藏。
4. 在图片节点下方出现「多角度编辑器」浮层。
5. 浮层跟随图片节点位置，当画布缩放、拖动或窗口滚动时，浮层保持锚定在对应图片下方。

### 3.2 关闭

关闭方式：

- 点击浮层右上角关闭 icon。
- 按 `Esc`。
- 如果正在打开参数下拉，第一次按 `Esc` 先关闭下拉；再次按 `Esc` 关闭整个浮层。
- 生成成功后，浮层自动关闭。

关闭后：

- 图片节点退出角度控制编辑状态。
- 图片工具栏恢复到正常选中展示逻辑。
- 未提交的角度参数、提示词补充和下拉状态被丢弃。

## 4. 浮层整体布局

浮层采用项目内图片编辑器统一的深色玻璃风格：

- 宽度：桌面端约 760px。
- 位置：默认位于图片节点下方，和图片节点保持约 14px 间距。
- 圆角、阴影、背景、关闭 icon 风格应与其他图片编辑浮层保持一致。
- 浮层不跟随画布缩放而变大或变小，以屏幕固定尺寸展示。

结构从上到下为：

1. 顶部标题栏
2. 快捷视角 tab
3. 主编辑区
4. 错误提示区
5. 底部操作区

## 5. 顶部标题栏

标题栏左侧：

- compass 图标
- 文案：多角度编辑器

标题栏右侧：

- 关闭 icon
- hover 后显示 Tooltip「关闭」
- 生成中时关闭按钮禁用，避免提交过程中被中断导致状态不一致

## 6. 快捷视角

快捷视角以横向 tab / segmented button 的形式展示。

当前视角选项：

- 自定义
- 正侧面
- 近侧面75°
- 斜侧面45°
- 近正侧15°
- 正面俯拍
- 正面仰拍
- 侧面俯拍
- 侧面仰拍

交互规则：

- 点击预设后，该预设进入选中状态。
- 预设会同步修改水平环绕、垂直俯仰、相机距离。
- 用户拖拽 3D 相机或手动调整滑块后，预设状态自动切换为「自定义」。
- 生成中时所有预设按钮禁用。

选中状态：

- 背景高亮。
- 文字颜色增强。
- 当前选项通过 `aria-selected=true` 标识。

未选中状态：

- 透明或弱背景。
- hover 时轻微高亮。

## 7. 主编辑区

主编辑区分为左右两栏。

左侧是 3D 相机预览。

右侧是参数控制：

- 水平环绕
- 垂直俯仰
- 相机距离
- 提示词补充

### 7.1 3D 相机预览

3D 预览需要是真实的 3D 场景，而不是平面示意图。

预览中包含：

- 原图作为主体参考平面。
- 球形/轨道辅助线，用于表达相机围绕主体移动。
- 当前相机模型。
- 相机到主体的虚线视线。
- 地面网格，帮助用户理解空间方向。

交互方式：

- 用户在 3D 预览区域按住并拖拽，可以改变相机视角。
- 横向拖拽：调整水平环绕。
- 纵向拖拽：调整垂直俯仰。
- 四个方向微调按钮：
  - 向左旋转 15 度
  - 向右旋转 15 度
  - 向上移动相机 10 度
  - 向下移动相机 10 度

拖拽时：

- 需要阻止事件冒泡，避免拖动画布或拖动节点。
- 需要保持 3D 预览实时更新。
- 每帧合并参数变化，避免过度触发 React 更新。

禁用状态：

- 生成中时 3D 预览禁用。
- 禁用时预览透明度降低，不可拖拽，不可点击方向微调按钮。

### 7.2 水平环绕

控制相机围绕主体左右旋转。

参数范围：

- 最小值：-180°
- 最大值：180°
- 步进：1°
- 默认值：0°

含义：

- 0°：正面视角。
- 正数：相机绕主体向右移动，从主体右侧方向观察。
- 负数：相机绕主体向左移动，从主体左侧方向观察。

显示规则：

- 左侧文案：水平环绕
- 中间为滑块
- 右侧显示当前角度，例如 `45°`

### 7.3 垂直俯仰

控制相机上下移动形成俯视或仰视。

参数范围：

- 最小值：-60°
- 最大值：60°
- 步进：1°
- 默认值：0°

含义：

- 0°：平视。
- 负数：相机升高，形成俯视。
- 正数：相机降低，形成仰视。

显示规则：

- 左侧文案：垂直俯仰
- 中间为滑块
- 右侧显示当前角度，例如 `-30°`

### 7.4 相机距离

控制目标构图远近。

参数范围：

- 最小值：1
- 最大值：8
- 步进：0.1
- 默认值：4

距离文案映射：

- 1 到 2.5：特写
- 大于 2.5 且小于 6：中景
- 6 到 8：远景

显示规则：

- 左侧文案：相机距离
- 中间为滑块
- 右侧显示距离文案，不直接显示纯数字

### 7.5 提示词补充

用于补充主体、背景或构图要求。

输入框占位文案：

`补充主体、背景或构图要求...`

输入框规则：

- 支持多行输入。
- 默认 4 行高度。
- 生成中禁用。
- 用户可以留空；留空时仍然可以生成。
- 用户输入会作为「补充要求」追加到最终生成提示词中。

## 8. 底部操作区

底部左侧是「重置参数」按钮。

底部右侧是生成参数和生成按钮。

### 8.1 重置参数

按钮文案：

`重置参数`

点击后恢复：

- 快捷视角：自定义
- 水平环绕：0°
- 垂直俯仰：0°
- 相机距离：4
- 提示词补充：空
- 错误提示：清空

生成中时禁用。

### 8.2 图片 API

以 Popover 形式展示。

触发器显示：

- label：图片 API
- value：当前 API 名称
- 如果未配置，则显示：未配置 API

点击后下拉展示可用图片 API 列表。

选中状态：

- 当前 API 行高亮。
- 右侧显示 check icon。

未配置状态：

- 下拉中显示「未配置 API」。
- 生成按钮禁用。

### 8.3 图片模型

以 Popover 形式展示。

触发器显示：

- label：图片模型
- value：当前模型名称
- 如果没有模型，则显示：选择模型

点击后下拉展示当前 API 下可用图片模型。

选中状态：

- 当前模型行高亮。
- 右侧显示 check icon。

无模型状态：

- 下拉中显示「暂无模型」。
- 生成按钮禁用。

### 8.4 角度生成参数

以 Popover 形式展示。

触发器显示：

- label：角度生成参数
- value：比例、尺寸、张数的摘要
- 示例：`智能 · 2K · 1张`

下拉内容包含：

1. 比例
2. 尺寸
3. 张数

比例：

- 使用模型能力支持的比例列表。
- 如果模型无能力配置，则使用项目内图片比例预设。
- 当前比例按钮高亮。

尺寸：

- 使用模型能力支持的尺寸列表。
- 常见值：1K、2K、4K。
- 当前尺寸按钮高亮。

张数：

- 可选：1张、2张、3张、4张。
- 当前张数按钮高亮。

### 8.5 生成按钮

生成按钮位于底部最右侧。

正常状态：

- play icon
- hover 显示 Tooltip「生成新角度」
- `aria-label` 为「生成新角度图片」

生成中状态：

- loader icon
- 按钮禁用
- 所有参数输入禁用

禁用条件：

- 正在生成中
- 没有可用图片 API
- 没有可用图片模型

## 9. 生成逻辑

点击生成后：

1. 校验当前图片来源必须是图片结果节点。
2. 校验来源节点仍然存在。
3. 校验所选 API 和模型可用。
4. 如果 API 协议有能力表，并且所选模型不支持 image-to-image，则提示错误。
5. 根据当前角度参数构建生成提示词。
6. 在当前图片节点下游创建新的图片生成节点。
7. 将当前图片作为参考图传入新生成节点。
8. 自动触发图片生成任务。
9. 生成成功提交后关闭多角度编辑器。

生成结果：

- 不覆盖原图。
- 不替换当前图片。
- 新建一个下游图片节点。
- 新节点保留角度控制相关元数据，便于后续回溯。

需要写入新节点/生成任务的数据：

- 原始图片 URL
- 来源节点 ID
- 来源图片 index
- 预设 ID
- 水平环绕角度
- 垂直俯仰角度
- 相机距离
- 生成提示词
- 图片模型
- 图片 API
- 输出比例
- 输出尺寸
- 输出张数

## 10. 提示词规则

最终提示词由系统根据参数自动构造，用户补充提示词只作为其中一段。

提示词核心要求：

- 基于参考图生成同一主体的新视角图片。
- 第一张图片是原始主体参考。
- 保持身份、结构、材质、服装、颜色、风格和光照一致。
- 目标视角根据水平环绕和垂直俯仰自动描述。
- 主体自身不要旋转，不要改变姿态。
- 不可见区域需要合理补全。
- 构图保持单主体清晰居中。
- 不要新增无关主体、文字、水印、边框或多余装饰。

角度描述规则：

- 水平环绕 0°：相机保持在主体正前方观察。
- 水平环绕大于 0：相机绕主体向右移动到对应角度，从主体右侧方向观察。
- 水平环绕小于 0：相机绕主体向左移动到对应角度，从主体左侧方向观察。
- 垂直俯仰 0°：保持平视。
- 垂直俯仰大于 0：相机降低，形成仰视。
- 垂直俯仰小于 0：相机升高，形成俯视。

## 11. 错误状态

错误提示显示在主编辑区和底部操作区之间。

常见错误：

- 没有可用的参考图编辑模型，请先在设置里配置图片模型。
- 当前图片不支持视角调整。
- 来源图片已不存在。
- 当前 API 的参考图修改不支持此图像模型，请切换支持参考图的模型后再生成。
- 角度控制生成提交失败。

错误出现后：

- 浮层保持打开。
- 生成中状态恢复为 false。
- 用户可以修改参数后再次生成。

## 12. 与图片工具栏其他功能的互斥关系

角度控制和以下模式互斥：

- 标注
- 局部修改
- 擦除
- 打光
- 旋转
- 裁剪
- 宫格拆分
- 扩图/抠图/增强/调整像素等原型编辑浮层

打开角度控制时，应关闭上述其他编辑状态。

打开上述其他编辑状态时，也应关闭角度控制。

## 13. 画布交互规则

角度控制浮层打开时：

- 不允许点击浮层触发画布选择。
- 不允许拖拽 3D 预览时拖动画布。
- 不允许滚动参数区域时误触发画布缩放。
- 浮层区域需要带 `nodrag`、`nopan` 行为。
- 图片节点处于图片编辑状态，避免外部点击误触发普通工具栏。

节点拖拽中：

- 角度控制入口工具栏不显示。
- 已打开的工具浮层应被视为编辑态，不应跟随拖拽产生误操作。

## 14. 响应式规则

桌面端：

- 浮层约 760px 宽。
- 左侧 3D 预览，右侧参数控制。
- 底部操作横向排列。

窄屏：

- 浮层宽度收缩到视口内。
- 主编辑区改为上下排列。
- 3D 预览高度降低。
- 底部操作允许换行。
- 图片 API、图片模型、角度生成参数三个控制项可以换行排列。

## 15. 验收标准

### 15.1 入口验收

- 选中图片节点后可以看到角度控制入口。
- hover 角度控制按钮显示 Tooltip「角度控制」。
- 非图片结果节点不出现角度控制入口。
- 节点拖拽中不出现图片工具栏。

### 15.2 浮层验收

- 点击角度控制后出现「多角度编辑器」。
- 浮层出现在图片节点下方。
- 浮层不随画布缩放变大或变小。
- 关闭 icon 可关闭浮层。
- `Esc` 可关闭浮层或当前打开的下拉。

### 15.3 3D 预览验收

- 预览不是空白 canvas。
- 能看到主体参考图、相机、轨道、视线和地面网格。
- 拖拽预览可以改变水平环绕和垂直俯仰。
- 点击四个方向按钮可以微调视角。
- 生成中预览不可拖拽。

### 15.4 参数验收

- 快捷视角点击后会同步改变滑块。
- 手动拖拽或滑块调整后，视角切换为自定义。
- 水平环绕范围为 -180° 到 180°。
- 垂直俯仰范围为 -60° 到 60°。
- 相机距离范围为 1 到 8，并显示特写/中景/远景。
- 提示词补充可为空。

### 15.5 生成验收

- 未配置 API 或模型时不能生成。
- 点击生成后按钮变为 loading 状态。
- 成功提交后关闭浮层。
- 生成结果是新的下游图片节点。
- 原图片节点内容不被覆盖。
- 新生成任务携带原图作为参考图。

## 16. 后续可优化项

- 在底部显示本次角度生成所需积分。
- 在 3D 预览中增加当前角度数值标签。
- 支持左右对称视角一键切换。
- 支持保存常用自定义视角。
- 支持多角度批量生成。
- 支持在生成结果标题右侧显示来源角度标签，例如「侧面75°」。
