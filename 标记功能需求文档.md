# 画布节点标记功能需求文档

## 1. 背景

在无限画布中，用户会创建大量图片、视频、文本、分组等节点。随着节点数量增加，用户需要一种轻量方式对节点进行分类、重点标注和快速定位。

「标记」功能用于给画布中的节点添加颜色标签。一个节点可以添加多个颜色标记。画布顶部会汇总当前已经使用过的标记颜色，用户可以通过顶部颜色入口快速查看、筛选和定位相关节点。

## 2. 目标

- 支持对任意节点添加一个或多个颜色标记。
- 节点被标记后，节点标题后方显示对应颜色点。
- 画布顶部统一展示当前画布已使用的标记颜色。
- 点击顶部颜色点后，高亮所有拥有该颜色标记的节点。
- 支持查看该颜色下的节点列表，并点击列表项定位到对应节点。
- 支持修改颜色标记的显示名称。

## 3. 使用场景

### 3.1 分类管理

用户可以用不同颜色区分节点类型，例如：

- 红色：需要修改
- 黄色：待确认
- 绿色：已完成
- 蓝色：重点参考

### 3.2 快速定位

当画布中节点很多时，用户可以点击顶部颜色点，查看所有被该颜色标记的节点，并快速跳转到目标节点。

### 3.3 多维标记

一个节点可能既是「重点参考」，又是「待确认」，因此同一个节点需要支持多个颜色标记。

## 4. 功能范围

### 4.1 节点工具栏增加「标记」

所有支持工具栏的节点，都需要提供「标记」入口。

点击「标记」后，出现颜色选择菜单。菜单中展示固定颜色列表，用户点击某个颜色后：

- 如果节点还没有该颜色，则添加该颜色标记。
- 如果节点已经有该颜色，则取消该颜色标记。

该交互是开关式操作，不需要二次确认。

### 4.2 节点标题显示标记颜色

节点添加标记后，在节点名称后方显示对应颜色点。

规则：

- 一个颜色显示一个小圆点。
- 多个颜色按固定颜色顺序排列。
- 颜色点只表达标记状态，不承载额外操作。
- 未添加标记时，不显示颜色点。

### 4.3 画布顶部标记汇总

画布顶部显示当前画布中已经被使用的颜色点。

规则：

- 只展示已被至少一个节点使用的颜色。
- 每个颜色点代表一种标记颜色。
- hover 颜色点时，显示该颜色名称和对应节点数量。
- 点击颜色点后，打开该颜色的下拉面板。

### 4.4 标记颜色下拉面板

点击顶部颜色点后，出现下拉面板。

面板内容包括：

- 当前颜色名称。
- 当前颜色关联的节点数量。
- 修改颜色名称的入口。
- 该颜色下的节点列表。

节点列表中，每一项展示节点名称。点击某个节点后：

- 画布自动定位到该节点。
- 该节点进入选中状态。
- 关闭当前下拉面板。
- 清除顶部颜色点的临时选中态。

### 4.5 节点高亮

点击顶部颜色点时，所有拥有该颜色标记的节点需要显示对应颜色描边。

规则：

- 高亮颜色与标记颜色一致。
- 只高亮当前选中的一种颜色。
- 点击画布空白处后，关闭下拉面板，并取消顶部颜色点选中态和节点高亮。
- 再次点击当前已选中的颜色点，可以关闭下拉面板并取消高亮。

### 4.6 修改标记名称

用户可以在顶部颜色下拉面板中修改颜色标记的显示名称。

规则：

- 修改的是颜色标记的名称，不是颜色本身。
- 修改后，顶部 hover 文案和下拉标题同步更新。
- 不影响节点已有标记关系。
- 如果名称为空，恢复为默认颜色名称。

## 5. 标记颜色

默认提供固定颜色集合：

| 颜色 | 默认名称 | 用途示例 |
| --- | --- | --- |
| 红色 | 红色 | 需要修改、风险点 |
| 橙色 | 橙色 | 优先处理 |
| 黄色 | 黄色 | 待确认 |
| 绿色 | 绿色 | 已完成 |
| 蓝色 | 蓝色 | 重点参考 |
| 紫色 | 紫色 | 创意方向 |

颜色集合暂时固定，不支持用户新增自定义颜色。

## 6. 数据规则

### 6.1 节点标记数据

每个节点保存自己的标记颜色列表。

要求：

- 同一个节点不能重复保存相同颜色。
- 删除某个颜色标记后，该颜色从该节点的标记列表中移除。
- 如果节点没有任何标记，标记列表可以为空。

### 6.2 颜色名称数据

画布级别保存颜色名称。

要求：

- 颜色名称属于当前画布。
- 不同画布可以有不同的颜色名称。
- 修改颜色名称不影响颜色 ID，也不影响节点和颜色的关联关系。

## 7. 交互边界

### 7.1 删除节点

删除节点后：

- 该节点上的标记关系同步消失。
- 如果某个颜色已经没有任何节点使用，顶部不再显示该颜色点。

### 7.2 复制节点

复制节点时，建议复制原节点的标记颜色。

这样用户复制一组已分类节点时，不需要重新标记。

### 7.3 节点重命名

节点重命名后：

- 节点标题后方的颜色点保持不变。
- 顶部下拉面板中的节点名称应同步更新。

### 7.4 点击画布空白处

如果标记下拉面板已打开，点击画布空白处后：

- 关闭下拉面板。
- 取消顶部颜色点选中态。
- 取消节点颜色描边高亮。

这是为了避免用户看到「浮层消失了，但顶部颜色点仍然像选中」的误解。

## 8. 非目标范围

当前版本不包含以下能力：

- 不支持新增自定义颜色。
- 不支持删除系统默认颜色。
- 不支持跨画布共享标记名称。
- 不支持批量给多个节点添加标记。
- 不支持通过标记进行真实过滤隐藏，只做高亮和定位。
- 不支持标记权限或多人协作冲突处理。

## 9. 验收标准

### 9.1 节点标记

- 在节点工具栏点击「标记」，可以看到颜色菜单。
- 点击某个颜色后，节点标题后方出现对应颜色点。
- 再次点击同一颜色后，该颜色点从节点标题后方消失。
- 一个节点可以同时显示多个颜色点。

### 9.2 顶部汇总

- 当画布中存在已标记节点时，顶部显示对应颜色点。
- 未被任何节点使用的颜色，不显示在顶部。
- hover 顶部颜色点，可以看到颜色名称和节点数量。

### 9.3 高亮与定位

- 点击顶部颜色点后，所有拥有该颜色的节点显示同色描边。
- 下拉面板中展示所有拥有该颜色的节点。
- 点击节点列表项后，画布定位到对应节点。
- 定位后，下拉面板关闭，顶部颜色点选中态取消。

### 9.4 关闭状态

- 打开颜色下拉后，点击画布空白处，下拉面板关闭。
- 关闭后，顶部颜色点不再保持选中态。
- 关闭后，节点颜色描边高亮消失。

### 9.5 修改名称

- 可以在颜色下拉面板中修改颜色名称。
- 修改后，顶部 hover 文案和下拉标题展示新名称。
- 清空名称后，恢复默认名称。

## 10. 后续可扩展方向

后续如果用户使用频率较高，可以考虑增加：

- 批量标记多个选中节点。
- 根据标记筛选显示节点。
- 自定义新增颜色。
- 标记搜索。
- 标记统计面板。
- 协作场景下显示谁添加了标记。

